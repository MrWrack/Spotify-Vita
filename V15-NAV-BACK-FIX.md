# v15 — D-pad + Back fix

Login:
- D-pad UP/DOWN moves between LOGIN and BACK.
- X activates the selected item.
- O works as Back.
- Touch works on both LOGIN and BACK.

Error screen:
- O or X returns to the login screen instead of trapping the user.

Other screens:
- Existing O -> Home behavior is retained.

This build intentionally does not change Spotify Client ID/auth configuration.
